# Copy to .env in this directory (relay/x402-facilitator)
# Never commit .env

# Option A: keypair file path
RELAY_FACILITATOR_KEYPAIR_PATH=./facilitator-wallet.json

# Option B: JSON array of bytes (same as file contents)
# RELAY_FACILITATOR_SECRET_KEY=[1,2,3,...]
